// This file is intentionally ignored by the TanStack Router Vite plugin
// (routeFileIgnorePattern: "_auth/index" in vite.config.ts).
// The public landing page at routes/index.tsx owns "/",
// and authenticated users are redirected to /dashboard from there.
export {};
